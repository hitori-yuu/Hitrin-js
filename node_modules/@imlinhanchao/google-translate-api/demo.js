const tran = require('./index')

async function main () {
    let res = await tran('The language in which the text should be translated.', { to: 'zh-cn', client: 't' });
    console.dir(res);
}

main()